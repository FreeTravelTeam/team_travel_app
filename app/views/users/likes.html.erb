


<% @likes.articles.each do |article| %>
   <h4 class="user">
       <span class="user-email">
           <link_to article.title, article_path(article.id) %>
       </span>
    <span class="user-id"><%= article.user.email %></span>
   </h4>
   <!--<div class="article-image">-->
   <!--    <%= image_tag article.image.url %>-->
   <!--</div>-->
 <% end %>