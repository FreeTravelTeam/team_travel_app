
<section class="iamges-sec">
	<% @articles.each do |article| %>
	<div class="index-iamges">
		<div class="index-image">
			<%= image_tag article.image.url, class: "img" %>
		</div>
		<p><%= link_to "Destroy", article_path(article), method: :delete %></p>
	</div>
	<% end %>
</section>